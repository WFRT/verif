language: python
python:
  - "2.6"

install:
   - pip install matplotlib
   - pip install numpy
   - pip install scipy

script:
   - sudo python setup.py build
   - sudo python setup.py install
   - python -m verif.testing

env:
 - DIST=lucid

os: linux
